import { Genome } from './genome';

describe('Genome', () => {
  it('should create an instance', () => {
    const data = {}; // Define 'data' with appropriate structure
    expect(new Genome(data)).toBeTruthy();
  });
});
